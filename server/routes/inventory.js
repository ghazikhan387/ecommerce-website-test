const express = require('express');
const router = express.Router();

// Placeholder - will be fully implemented after server boots
// Placeholder - will be fully implemented after server boots
router.get('/stock', (req, res) => {
  res.json([]);
});

router.get('/alerts/low-stock', (req, res) => {
  res.json([]);
});

module.exports = router;
